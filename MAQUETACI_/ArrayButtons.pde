/*int n;
class ArrayButtons {
  
  Button[] buttons;
  
  ArrayButtons(int n, float x, float y, float r, float s){
    buttons = new Button[n];
    for(int i=0; i<n; i++){
      buttons[i] = new Button(Integer.toString(i), x + (s+r)*i, y, r, r);
    }
  }
  
  void display(){
    pushStyle();
    for(int i=0; i<n; i++){
      buttons[i].displayButton();
    }
    popStyle();
  }
}*/
